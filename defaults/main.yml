# Standards: 0.1
---
# @var spotify_user:description: User to run user-specific commands
# @var spotify_user:value: spotify_user | default(homebrew_user) | default(ansible_user_id)
# spotify_user:

# @var spotify_started:description: Restart app if already running
spotify_started: True

...
